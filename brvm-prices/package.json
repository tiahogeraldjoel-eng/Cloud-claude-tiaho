{
  "name": "brvm-prices",
  "version": "1.0.0",
  "description": "Cloudflare Worker — BRVM stock prices CORS proxy",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
